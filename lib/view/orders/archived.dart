import 'package:flutter/material.dart';

class OrdersArchived extends StatelessWidget {
  const OrdersArchived({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Archived Orders"),
      ),
    );
  }
}
